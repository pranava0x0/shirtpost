# Copy to backend/.env and fill in. NEVER commit the real .env (see .gitignore).
# List-typed vars are JSON-encoded.

# --- Database ---
DATABASE_URL=sqlite:///./shirtpost.db

# --- Security / network ---
ALLOWED_HOSTS=["localhost","127.0.0.1"]
ADMIN_CORS_ORIGINS=["http://localhost:3000"]

# --- Radar ---
RADAR_ENABLED=true
RADAR_POLL_INTERVAL_SECONDS=900
# Sources: "simulated" (no network), "google_trends", "reddit"
RADAR_SOURCES=["simulated"]

# --- Printful ---
PRINTFUL_API_KEY=
PRINTFUL_STORE_ID=
PRINTFUL_DEFAULT_PRODUCT_ID=71
PRINTFUL_DEFAULT_VARIANT_ID=4012
# Public base URL that hosts generated SVG print files (Printful fetches by URL).
# Leave empty until hosting exists; the Factory will fail loud and save the SVG locally.
PRINTFUL_PRINT_FILE_BASE_URL=

# --- X.com (OAuth 1.0a user context) ---
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
